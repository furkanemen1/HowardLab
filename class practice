class Point { 
     x: number;
     y: number;

     constructor(x?: number, y?: number) {
         this.x = x;
         this.y = y;
     }

     draw() {
        console.log('X: ' + this.x + ', Y: ' + this.y);  //.. drawing code logic is here}
     }

     getDistance(another: Point) {
//learning methods

     }
}

let point = new Point(1,2);
point.x = 1;
point.y = 2;
point.draw();
